version: '3.8'

name: dissertation
services: 
    api-service:
        build: ./api/
        volumes: 
            - ./api/:/usr/src/app/
        ports: 
            - 5000:5000
        environment: 
            PORT: 5000
        depends_on:
            - influxdb
        links:
            - influxdb

    influxdb:
        build: ./influxdb/
        expose:
            - "8086"
        volumes:
            - './influxdb/:/var/lib/influxdb'



        
        
